name: umtp-responder
on:
  push:
    branches: [main]
    paths: ['umtp-responder/**', 'oci/**', '.github/workflows/umtp-responder.yml', '.github/workflows/_package-image.yml', 'toolchain.env']
  workflow_dispatch:
permissions:
  contents: read
  packages: write
  id-token: write
jobs:
  build:
    uses: ./.github/workflows/_package-image.yml
    secrets: inherit
    with:
      package: umtp-responder
